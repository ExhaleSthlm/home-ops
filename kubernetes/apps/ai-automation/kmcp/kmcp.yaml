---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/gitrepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: kmcp
  namespace: cozy-fluxcd
spec:
  interval: 1h
  ref:
    branch: main
  url: https://github.com/kagent-dev/kmcp.git
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kmcp-crds
  namespace: ai-automation
  annotations:
    lineage.cozystack.io/ignore: "true"
spec:
  interval: 1h
  chart:
    spec:
      chart: ./helm/kmcp-crds
      sourceRef:
        kind: GitRepository
        name: kmcp
        namespace: cozy-fluxcd
      interval: 10m
  install:
    remediation:
      retries: -1
    crds: Create
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
    crds: CreateReplace
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kmcp
  namespace: ai-automation
  annotations:
    lineage.cozystack.io/ignore: "true"
spec:
  interval: 1h
  dependsOn:
    - name: kmcp-crds
      namespace: ai-automation
  chart:
    spec:
      chart: ./helm/kmcp
      sourceRef:
        kind: GitRepository
        name: kmcp
        namespace: cozy-fluxcd
      interval: 10m
  install:
    remediation:
      retries: -1
    crds: CreateReplace
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
    crds: CreateReplace
  values:
    image:
      repository: ghcr.io/kagent-dev/kmcp/controller
      tag: "0.2.8@sha256:7f75155e144c072ba0b7f319b86d64ac27938bcfd8a0d150bf07079dcc3d0350"
      pullPolicy: IfNotPresent
    controller:
      replicaCount: 1
    resources:
      limits:
        cpu: 500m
        memory: 128Mi
      requests:
        cpu: 10m
        memory: 64Mi
